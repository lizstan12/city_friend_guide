<%= form_with(model: @entry, local: true) do |f| %>

  <%= f.hidden_field :creator_id, value: @user.id %>

  <div class="form-group">
    <%= f.label :neighborhood %>

    <%= f.text_field :neighborhood, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :cityguide_id %>

    <%= f.collection_select :cityguide_id, CityGuide.all, :id, :to_s, {}, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :geo_location %>

    <%= f.text_field :geo_location, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :category_id %>

    <%= f.collection_select :category_id, Category.all, :id, :to_s, {}, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.label :journal %>
    <%= f.text_area :journal, rows: "3", class: "form-control" %>
  </div>

  <%= f.submit "Create entry", class: "btn btn-block btn-outline-secondary" %>
<% end %>
